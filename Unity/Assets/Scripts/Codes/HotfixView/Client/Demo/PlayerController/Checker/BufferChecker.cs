namespace ET.Client
{
    public class BufferChecker: CheckerHandler
    {
        public override int Execute(Unit player, CheckerConfig config)
        {
            // Input.Instance.CheckInput(OperaType)
            return 0;
        }
    }
}